<%= form_for(offline_payment) do |f| %>
  <% if offline_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(offline_payment.errors.count, "error") %> prohibited this offline_payment from being saved:</h2>

      <ul>
      <% offline_payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cash %>
    <%= f.text_field :cash %>
  </div>

  <div class="field">
    <%= f.label :check %>
    <%= f.text_field :check %>
  </div>

  <div class="field">
    <%= f.label :customer_id %>
    <%= f.text_field :customer_id %>
  </div>

  <div class="field">
    <%= f.label :plan_id %>
    <%= f.text_field :plan_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
