<%= form_for(plan) do |f| %>
  <% if plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plan.errors.count, "error") %> prohibited this plan from being saved:</h2>

      <ul>
      <% plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :plan_name %>
    <%= f.text_field :plan_name %>
  </div>

  <div class="field">
    <%= f.label :plan_price %>
    <%= f.text_field :plan_price %>
  </div>

  <div class="field">
    <%= f.label :plan_start_date %>
    <%= f.date_select :plan_start_date %>
  </div>

  <div class="field">
    <%= f.label :plan_expiry_date %>
    <%= f.date_select :plan_expiry_date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
